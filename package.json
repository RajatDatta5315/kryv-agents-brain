{
  "name": "kryv-agents-brain",
  "version": "2.0.0",
  "description": "KRYV Agent Brain — PC-primary server + web dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "pm2": "pm2 start ecosystem.config.js",
    "frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0"
  }
}
